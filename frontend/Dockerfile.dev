FROM node:18-slim

WORKDIR /app

# Copy package files and install dependencies
COPY package*.json ./
RUN npm install

# Source files are mounted as a volume at runtime
# so we don't COPY . . here

EXPOSE 5173

CMD ["npm", "run", "dev"]
